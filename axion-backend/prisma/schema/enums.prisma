enum Role {
    USER
    ADMIN
}
enum UserStatus {
  ACTIVE
  INACTIVE
  BLOCKED
  DELETED
}

enum Visibility {
  PUBLIC
  UNLISTED
  PRIVATE
}

enum EnrollmentStatus {
  PENDING
  ACTIVE
  COMPLETED
  CANCELLED
}